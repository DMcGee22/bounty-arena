# Copy to deploy/local.env (gitignored) and edit if needed:
#   cp deploy/local.env.example deploy/local.env
#
# Defaults already match the current box — only change if IP/key move.

EC2_HOST=ubuntu@54.224.28.249
EC2_KEY="$HOME/Downloads/Bounty Arena Key.pem"
BRANCH=main
